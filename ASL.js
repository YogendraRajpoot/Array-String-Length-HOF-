// Given an array of strings print the length of each string (map)

const arr = ["desk" ,"chair" , "book" , "beaker" ,"pencil" , "calculator" , "clock" , "blackboard"]
 const arrayString = x => x.length
console.log(arr.map(arrayString))